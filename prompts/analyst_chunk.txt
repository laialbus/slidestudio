You are analysing one chunk of an academic document. Use the global document structure below as context so your extraction is consistent with the whole document.

GLOBAL DOCUMENT STRUCTURE:
$global_skeleton

CHUNK $chunk_index:
$chunk_text

Extract key information from this chunk and return a JSON object with these fields:
- title: document title (from global structure)
- document_type: one of "research_paper", "textbook", "lecture_notes", "other"
- technical_level: one of "beginner", "intermediate", "advanced"
- core_thesis: central argument of the document
- key_concepts: list of key concepts introduced or discussed in this chunk (at least 1)
- sections: list of sections covered by this chunk, each with:
    - heading: the section heading this content belongs to
    - importance: one of "high", "medium", "low"  ← required; NOT "level" or "position"
    - summary: what this chunk covers in 1-3 sentences  ← required; NOT "level" or "position"
  (The global structure above has "level" and "position" fields — those are input context only.
   Your output sections must have "importance" and "summary" instead.)
- figure_purposes: for each [FIGURE_ID: N] marker present in this chunk, classify the reference as:
    - "conceptual": the figure illustrates a design, mechanism, architecture, or system component
      that is being actively explained in the surrounding text (e.g. "...the offloading policy,
      as shown in Figure 6..." or "Figure 3 depicts the encoder-decoder structure...").
    - "evidential": the figure presents performance data, measurements, or results that support
      a claim (e.g. "Table 3 shows throughput improves..." or "as seen in Figure 8, accuracy
      drops under high load...").
  Use the integer figure ID as the JSON key (e.g. {"6": "conceptual", "8": "evidential"}).
  If no [FIGURE_ID: N] markers are present, return {}.

If the chunk contains [FIGURE EXCLUDED: ...] or [TABLE EXCLUDED: ...] markers, reference them explicitly in the relevant section summary so downstream agents are aware of the omitted visual content.

If the chunk contains [EQUATION: ...] markers, note in the relevant section summary that a formal equation was present at that position and describe what the surrounding text indicates it represents. Do not attempt to reproduce the equation — describe its purpose and what quantities the context says it relates.

Return ONLY the JSON object. No markdown fences, no explanation.
